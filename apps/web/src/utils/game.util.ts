import type { GameOutcome } from 'types/game.types';

// checkOutcome should return the outcome of the game based on the current game state
//
export const checkOutcome = (): GameOutcome => {
  return 'unresolved';
};
